<div class="container">
 <p id="notice"><%= notice %></p>

<h1>
  <strong>Name:</strong>
  <%= @song.name %>
</h1>

<h3>
  <strong>Artist:</strong>
  <%= link_to @song.artist.try(:name), artist_path %>
</h3>

<%= link_to 'Edit', edit_song_path(@song), class: 'btn btn-info' %> |
<%= link_to 'Back', songs_path, class: 'btn btn-info' %>
</div>